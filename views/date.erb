

<!DOCTYPE html>
  <html>
    <head>
        <title>date Page</title>
    </head>
    <body>
        <h1>Today</h1>
        <h2> </h2>
        <p><%time = DateTime.now%>The date is <%= time.strftime('%A')%>, <%= time.strftime("%B")%> <%= time.strftime("%d")%>, <%= time.strftime("%Y")%></p>
    </body>
  </html>
